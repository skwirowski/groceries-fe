import { Button } from "@mui/material";

export default function ButtonSubmit() {
  return (
    <Button
      variant="contained"
      size="large"
      fullWidth
      sx={{
        my: 3,
      }}
    >
      Sign up
    </Button>
  );
}
