import { Button } from "@mui/material";

export default function ButtonSubmit() {
  return (
    <Button
      variant="contained"
      size="large"
      fullWidth
      sx={{
        my: 4,
      }}
    >
      Sign up
    </Button>
  );
}
